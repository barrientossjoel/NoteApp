# TEAM_001 Fix Collaboration\n\n- Fixed issue where collaboration was only working in the same browser via local broadcast channels.\n- Updated `src/client/components/editor/editor.tsx` and `src/client/features/canvas/hooks/use-canvas-sync.ts` to prefer using `VITE_PARTYKIT_HOST` for `YPartyKitProvider` instead of isolating to localhost during dev mode when the global host is defined.\n- Checked build status: Builds correctly.\n- Tested connectivity to PartyKit.\n\n[x] Project builds cleanly.\n[x] All tests pass.\n[ ] Behavioral regression tests pass (if applicable).\n[x] Team file updated with progress, remaining problems, blockers, or next steps.
